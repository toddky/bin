
set-option -g status off

set-option -g pane-base-index 1

#bind-key -T dashboard q detach-client
bind-key -T dashboard q kill-session

bind-key -T dashboard h select-pane -L
bind-key -T dashboard j select-pane -D
bind-key -T dashboard k select-pane -U
bind-key -T dashboard l select-pane -R

bind-key -T dashboard v split-pane -h
bind-key -T dashboard s split-pane -v

set-option -gs key-table dashboard

